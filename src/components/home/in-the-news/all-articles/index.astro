---

// IMPORTS ASTRO COMPONENTS
import Article from "@/components/home/in-the-news/all-articles/article/index.astro"

// GET ASTRO PROPS
const {

    CDN,
    data,
    PROJECT

} = Astro.props

---
<div class="grid md:grid-cols-2 gap-x-14 gap-y-8 p-6 md:p-16 bg-white rounded-3xl" style="box-shadow: 0 3px 4px 0 rgba(0,0,0,.14), 0 3px 3px -2px rgba(0,0,0,.12), 0 1px 8px 0 rgba(0,0,0,.2)">
    {

        data.map( ( value ) => {

            return(
                <Article
                    alternative_text={ value.title }
                    CDN={ CDN }
                    href={ value.link }
                    PROJECT={ PROJECT }
                    source={ value.cover }
                    title={ value.title }
                />
            )

        })

    }
</div>